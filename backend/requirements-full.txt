-r requirements.txt

# Natural realtime TTS, preferred engine when installed (GPU or CPU).
# Best results with the espeak-ng system package installed.
kokoro>=0.9.4
soundfile>=0.12

# Kyutai bidirectional speech (streaming STT, plus fallback TTS)
moshi>=0.2.6
sphn>=0.1.4
sentencepiece>=0.2.0

# Better RAG embeddings (falls back to hashed embeddings without this)
sentence-transformers>=3.0

# Camera behavior analysis (eye gaze, head pose, expressions)
mediapipe>=0.10.14
opencv-python-headless>=4.9
