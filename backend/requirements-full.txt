-r requirements.txt

# Kyutai bidirectional speech (STT + TTS, streaming)
torch>=2.2
moshi>=0.2.6
sphn>=0.1.4
sentencepiece>=0.2.0

# Local Hugging Face LLM inference
transformers>=4.42
accelerate>=0.30

# Better RAG embeddings (falls back to hashed embeddings without this)
sentence-transformers>=3.0

# Camera behavior analysis (eye gaze, head pose, expressions)
mediapipe>=0.10.14
opencv-python-headless>=4.9
